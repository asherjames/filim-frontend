export default class Film {
    constructor(key, posterImage, overview, date, title, backdropImage) {
        this.key = key
        this.posterImage = posterImage
        this.overview = overview
        this.date = date
        this.title = title
        this.backdropImage = backdropImage
    }
}